<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.explore.nmerp.ictblog">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Add_new_post" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginPage" />
        <activity android:name=".SetUpAccount" />
        <activity android:name=".SinglePost" />
        <activity android:name=".HomePage" />
        <activity android:name=".MyProfile" />
        <activity android:name=".UserBlogPost" />
        <activity android:name=".Edit_Post"></activity>
    </application>

</manifest>